name = "serabut-og"
main = "og-worker.js"
compatibility_date = "2024-01-01"

[vars]
# No secrets needed — GAS URL is public

# Route: semua traffic serabut.id lewat worker ini
# (set di CF dashboard: Workers Routes → serabut.id/produk/* → serabut-og)
